archivesBaseName = "aciertoteam-osm"
description = "Wrapper around open osm api"

dependencies {
    compile project(':aciertoteam-common')

    compile commons_lang3
    compile commons_io
    compile jodd
    compile spring_beans

    testCompile project(':aciertoteam-test')
}