archivesBaseName = "aciertoteam-generator"
description = "Generates output by specific rules based on provided input data"

dependencies {
    compile project(':aciertoteam-io')

    testCompile junit
}